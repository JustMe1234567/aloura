'use client';
import {useState} from 'react'; import Header from '../components/Header'; import ProductCard from '../components/ProductCard'; import {products} from '../data';
export default function Collections(){const[filter,setFilter]=useState('All');const cats=['All','Rings','Earrings','Necklaces','Bracelets'];const shown=filter==='All'?products:products.filter(p=>p.category===filter);return <main><Header light/><section className="collection-hero"><p className="section-kicker">The collection</p><h1>Objects of <em>affection.</em></h1><p>Signature forms, made in precious materials and designed for every day.</p></section><section className="catalog"><div className="catalog-tools" aria-label="Product filters">{cats.map(c=><button key={c} className={filter===c?'active':''} onClick={()=>setFilter(c)}>{c}</button>)}<span>{shown.length} pieces</span></div><div className="product-grid catalog-grid">{shown.map((p,i)=><ProductCard key={p.slug} product={p} index={i}/>)}</div></section></main>}
